<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= assigns[:page_title] || "Push Notification Service (PNS)" %></title>
    <link rel="shortcut icon" type="image/jpg" href="<%= Routes.static_path(@conn, "/images/wm-favicon.png") %>"/>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <%= csrf_meta_tag() %>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
          <ul>
            <%= if Plug.Conn.get_session(@conn, :user_id) == nil do %>
                <li><a href="<%= Routes.static_path(@conn, "/auth/google") %>">Login</a></li>
              <% else %>
                <li><a href="<%= Routes.static_path(@conn, "/logout") %>">Logout</a></li>
              <% end %>
          </ul>
        </nav>
        <a href="https://www.watermarkinsights.com/" class="phx-logo">
          <img src="<%= Routes.static_path(@conn, "/images/wm-logo.png") %>" alt="Watermark Logo"/>
        </a>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
